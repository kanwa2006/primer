{
  "name": "sample-ts-lib",
  "version": "0.1.0",
  "description": "A minimal TypeScript library for fixture-based testing",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0"
  }
}
