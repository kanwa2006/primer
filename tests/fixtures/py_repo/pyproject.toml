[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "samplelib"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["requests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
